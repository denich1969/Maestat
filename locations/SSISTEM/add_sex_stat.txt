!Если два числовых агрумента были переданы, запускаем цикл.!Если два нулевых агрумента небыли переданы - назначаем дефолтными целями игрока и NPC.
if ARGS[0]=0 and ARGS[1]=0: ARGS[0]=you && ARGS[1]=npc
!Перепроверяем, что оба индекса валидны (если был передан всего один аргумент, в этом случае - ничего не делаем.)
if ARGS[0]>0 and ARGS[1]>0:
	!Проверяем, являются-ли цели секс-партнёрами, если да, увеличиваем каунтер, если нет - инициализируем его.
	if sexpartner['<<ARGS[0]>>,<<ARGS[1]>>'] = 0:
		!Вызываем init_sex_partner, чтобы инициализировать статистику.
		gs 'init_sex_partner', ARGS[0], ARGS[1]
		gs 'init_sex_partner', ARGS[1], ARGS[0]
		jump 'end_mark'
	else
		:end_mark
		sexpartner['<<ARGS[0]>>,<<ARGS[1]>>']+=1
		sexpartner['<<ARGS[1]>>,<<ARGS[0]>>']+=1
	end
end